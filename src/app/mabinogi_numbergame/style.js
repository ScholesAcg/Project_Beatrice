export const container = {
    display: "flex",
    flexDirection: "column",
    alignItems: "center",
    justifyContent: "center",
    minHeight: "100vh",
    backgroundColor: "#f4f4f4",
    fontFamily: "Arial, sans-serif",
  };

  export const container_row = {
    display: "flex",
    flexDirection: "row",
    alignItems: "center",
    justifyContent: "center",
  };

  export const container_column = {
    display: "flex",
    flexDirection: "column",
    alignItems: "center",
    justifyContent: "center",
    marginRight: "2rem",
  };

  export const inputstyle = {
    marginTop: "0.5rem",
    marginBottom: "0.5rem",
    width: "100%",
  };

  export const title = {
    fontSize: "2rem",
    fontWeight: "bold",
    marginBottom: "20px",
  };
  
  export const grid = {
    display: "grid",
    gridTemplateColumns: "repeat(3, 1fr)", // 每行 3 個格子，每個格子的寬度 100px
    gridGap: "10px", // 格子之間的間距
  };